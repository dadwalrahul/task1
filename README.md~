# mean-stack-authentication-and-registration-with-passport
===========================================================

all the statics has been in the public folder so no need to bower 
if you wish you can have a look at the components and install using
bower and change the file structure accordingly

$ npm install (to install all packages in package.json)
$ npm update (to update all packages in package.json)

run the server using

$ node server.js 
